server.port=${PORT:8080}
spring.application.name=movieparty

# Allow local development by default. Override with your Vercel URL in production.
movieparty.cors.allowed-origins=http://localhost:5173,https://your-app.vercel.app
