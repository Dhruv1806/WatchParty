# server.port=${PORT:8080}
# spring.application.name=movieparty
server.address=0.0.0.0
server.port=8080
# Allow local development by default. Override with your Vercel URL in production.
movieparty.cors.allowed-origins=http://localhost:5173,http://11.46.161.237:5173
# Admin portal credentials supplied at runtime.
movieparty.admin.username=${MOVIEPARTY_ADMIN_USERNAME}
movieparty.admin.password=${MOVIEPARTY_ADMIN_PASSWORD}
