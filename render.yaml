services:
  - type: web
    name: movieparty-backend
    env: java
    buildCommand: cd backend && mvn clean package -DskipTests
    startCommand: java -jar backend/target/movieparty-backend-0.0.1-SNAPSHOT.jar
    envVars:
      - key: PORT
        value: 8080
